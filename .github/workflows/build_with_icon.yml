name: Build macOS App with Icon

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # מאפשר להריץ ידנית בלחיצת כפתור

jobs:
  build:
    runs-on: macos-latest

    steps:
    # 1. משיכת הקוד והקבצים שלך (כולל האייקון)
    - uses: actions/checkout@v3

    # 2. התקנת פייתון
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # 3. התקנת הספריות הדרושות
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller tk

    # 4. הקימפול עצמו - כאן הוספנו את האייקון
    - name: Build with PyInstaller
      run: |
        # הפקודה כוללת עכשיו את דגל האייקון
        pyinstaller --onefile --windowed --icon="3.icns" --name "SystemGuard" Install_Videonet_Block.py
# ... (שלב 4 הסתיים, הקובץ SystemGuard.app נוצר) ...# --- שלב העלאה משופר עם Python ו-Rclone ---
    
    - name: Upload to Google Drive via Rclone (Robust) 🚀
      env:
        # מעבירים את הסוד למשתנה סביבה
        GDRIVE_SA_BASE64: ${{ secrets.GDRIVE_CREDENTIALS_B64 }}
      run: |
        echo "🔧 Installing Rclone..."
        brew install rclone > /dev/null

        echo "🔓 Decoding credentials using Python..."
        # שימוש בפייתון לפענוח בטוח (מתמודד טוב יותר עם רווחים/שבירות שורה מהעתקה)
        python3 -c "import os, base64; open('service_account.json', 'wb').write(base64.b64decode(os.environ['GDRIVE_SA_BASE64']))"

        echo "⚙️ Configuring Rclone..."
        # יצירת קובץ קונפיגורציה ידני כדי למנוע שגיאות סביבה
        mkdir -p ~/.config/rclone
        cat <<EOF > ~/.config/rclone/rclone.conf
        [gdrive]
        type = drive
        scope = drive
        service_account_file = $(pwd)/service_account.json
        EOF

        echo "📡 Verifying connection..."
        # בדיקה שהקונפיגורציה עובדת (אם זה נכשל, נדע שהבעיה ב-JSON)
        rclone about gdrive:

        echo "🚀 Uploading..."
        # העלאת הקובץ
        rclone copy dist/SystemGuard.zip gdrive:SystemGuard/ --progress

        echo "✅ Done!"
